# 환경변수
.services/lambda-presign/.env

# Rust 빌드 아티팩트
/services/lambda-presign/target/
/services/lambda-presign/deploy/

# IDE / 에디터
*.idea/
*.vscode/

# OS 파일
.DS_Store


#배포
deploy/


# (선택) Cargo.lock
# └ 바이너리 애플리케이션이라면 커밋하는 걸 추천드립니다.
#    라이브러리용 프레임워크라면 무시해도 무방합니다.
# /services/lambda-presign/Cargo.lock
services/lambda-presign/.env
